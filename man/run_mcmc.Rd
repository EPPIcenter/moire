% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcmc.R
\name{run_mcmc}
\alias{run_mcmc}
\title{Sample from the target distribution using MCMC}
\usage{
run_mcmc(
  data,
  is_missing = FALSE,
  allow_relatedness = TRUE,
  thin = 1,
  burnin = 10000,
  samples_per_chain = 1000,
  verbose = TRUE,
  use_message = FALSE,
  eps_pos_alpha = 1,
  eps_pos_beta = 1,
  eps_pos_locus_alpha = 1,
  eps_pos_locus_beta = 2000,
  eps_neg_alpha = 1,
  eps_neg_beta = 1,
  r_alpha = 1,
  r_beta = 1,
  population_coi_p_alpha = 1,
  population_coi_p_beta = 1,
  population_coi_r_shape = 0.1,
  population_coi_r_rate = 10,
  population_coi_lam_shape = 0.1,
  population_coi_lam_rate = 10,
  population_coi_method = "nb",
  num_populations = 1,
  populations_prior = NULL,
  max_eps_pos = 2,
  max_eps_neg = 2,
  max_coi = 40,
  record_latent_genotypes = FALSE,
  marginal_ecoi = FALSE,
  ecoi_mu_rate = 0.1,
  ecoi_k_shape = 2,
  ecoi_k_rate = 1,
  num_chains = 1,
  num_cores = 1,
  pt_chains = 1,
  pt_grad = 1,
  num_threads = NULL,
  pt_num_threads = NULL,
  adapt_temp = TRUE,
  pre_adapt_steps = 25,
  temp_adapt_steps = 25,
  max_initialization_tries = 10000,
  max_runtime = Inf,
  initial_allele_frequencies = NULL,
  observation_model = c("binary", "counts")
)
}
\arguments{
\item{data}{Data to be used in MCMC, as generated by the \verb{load_*_data} functions}

\item{is_missing}{Boolean matrix indicating whether the observation
should be treated as missing data and ignored. Number of rows equals the
number of loci, number of columns equals the number samples. Alternatively,
the user may pass in FALSE if no data should be considered missing.}

\item{allow_relatedness}{Bool indicating whether or not to allow relatedness
within host}

\item{thin}{Positive Integer. How often to sample from mcmc,
1 means do not thin}

\item{burnin}{Positive Integer. Number of MCMC samples to
discard as burnin}

\item{samples_per_chain}{Positive Integer. Number of samples to take
after burnin}

\item{verbose}{Logical indicating if progress is printed}

\item{use_message}{Logical indicating if progress is printed using message or print}

\item{eps_pos_alpha}{Positive Numeric. Alpha parameter in
Beta distribution for eps_pos prior}

\item{eps_pos_beta}{Positive Numeric. Beta parameter in
Beta distribution for eps_pos prior}

\item{eps_pos_locus_alpha, eps_pos_locus_beta}{Positive Numerics. Alpha/Beta
parameters of the Beta prior on the per-locus (raw) false-positive rate used
only in \code{marginal_ecoi} mode. With the COI marginalized the false-positive
rate is weakly identified: a free rate lets polyclonal samples explain their
extra alleles as error and collapse to effectively monoclonal, which biases
the eCOI heterogeneity (\code{ecoi_k}) low. This prior anchors the pooled
per-locus rate near a small known assay rate, breaking that degeneracy. The
default \code{Beta(1, 2000)} is a strong, monotone (mode at 0) anchor near zero;
the anchor must be strongly informative to resist the likelihood pull, and
must keep its mode at 0 (use \code{alpha = 1}) when the assay rate is ~0. For an
assay with a known nonzero rate r, use a concentrated bump centered at r
(\code{alpha > 1}, \code{alpha / (alpha + beta) = r}, large \code{alpha + beta}). Ignored
unless \code{marginal_ecoi = TRUE}.}

\item{eps_neg_alpha}{Positive Numeric. Alpha parameter in
Beta distribution for eps_neg prior}

\item{eps_neg_beta}{Positive Numeric. Beta parameter in
Beta distribution for eps_neg prior}

\item{r_alpha}{Positive Numeric. Alpha parameter in Beta
distribution for relatedness prior. NOTE: ignored when
\code{marginal_ecoi = TRUE}; eCOI fitting integrates the discrete COI out under a
uniform relatedness law (the effective COI is the identified quantity). The
relatedness Beta(\code{r_alpha}, \code{r_beta}) is only meaningful for the legacy
sampler and for post-hoc COI recovery from the eCOI posterior.}

\item{r_beta}{Positive Numeric. Beta parameter in Beta
distribution for relatedness prior. See \code{r_alpha} (ignored for eCOI fitting).}

\item{population_coi_p_alpha}{Alpha for Beta prior on negative-binomial p (COI)}

\item{population_coi_p_beta}{Beta for Beta prior on negative-binomial p (COI)}

\item{population_coi_r_shape}{Shape for gamma prior on negative-binomial r (COI)}

\item{population_coi_r_rate}{Rate for gamma prior on negative-binomial r (COI)}

\item{population_coi_lam_shape}{Shape for gamma prior on Poisson rate (COI)}

\item{population_coi_lam_rate}{Rate for gamma prior on Poisson rate (COI)}

\item{population_coi_method}{Character. Method for modeling population COI.
For negative binomial model, accepts: "nb", "nbinom", "negbinom", "negative_binomial".
For Poisson model, accepts: "pois", "poisson".}

\item{num_populations}{Number of populations}

\item{populations_prior}{Prior probability for each population (length \code{num_populations} or 1)}

\item{max_eps_pos}{Numeric. Maximum allowed value for eps_pos}

\item{max_eps_neg}{Numeric. Maximum allowed value for eps_neg}

\item{max_coi}{Positive Numeric. Maximum allowed complexity of infection}

\item{record_latent_genotypes}{Logical indicating whether or not to record
the latent genotypes at each step of the MCMC. WARNING: This will increase
the size of the output object significantly.}

\item{marginal_ecoi}{Logical. If TRUE, sample the effective COI (eCOI) and
marginalize the discrete COI analytically instead of sampling (COI,
relatedness) jointly. Effective COI draws are returned in the \code{effective_coi}
element of each chain. Each population has its own continuous effective-COI
prior \code{e ~ 1 + Gamma(shape = k_p, rate = k_p / mu_plus_p)} (there is no
monoclonal atom at \code{e = 1}; monoclonality and the COI/relatedness split are
post-hoc quantities derived under a user-supplied COI assumption). Per-
population draws of \code{mu_plus} and \code{k} are returned in \code{ecoi_mu_plus} /
\code{ecoi_k}. Experimental; defaults to FALSE (legacy sampler).
NOTE: collapsing COI removes the implicit cap the discrete COI places on the
latent genotype size, which exposes an eps_neg<->COI identifiability
degeneracy. Use an informative false-negative prior (e.g. \code{eps_neg_beta}
substantially larger than \code{eps_neg_alpha}) and a modest \code{max_eps_neg}
(e.g. <= 0.2) when \code{marginal_ecoi = TRUE}; otherwise effective COI can run
away to \code{max_coi}.}

\item{ecoi_mu_rate}{Positive Numeric. Rate of the Exponential hyperprior on
each population's \code{mu_plus}, the mean \emph{excess} effective COI (mean eCOI =
1 + mu_plus). Only used when \code{marginal_ecoi = TRUE}.}

\item{ecoi_k_shape, ecoi_k_rate}{Positive Numerics. Gamma hyperprior
(shape, rate) on each population's \code{k}, the shape/heterogeneity of the
effective COI (coefficient of variation = 1/sqrt(k)). Only used when
\code{marginal_ecoi = TRUE}.}

\item{num_chains}{Number of independent MCMC replicates to run. When greater
than 1, replicates are run in parallel R worker processes (see \code{num_cores}).}

\item{num_cores}{Number of R worker processes when \code{num_chains > 1}
(\code{parallel::mclapply}). Each worker runs its own C++ MCMC and may use up to
\code{num_threads} compute threads.}

\item{pt_chains}{Number of parallel-tempering chains, or a numeric vector of
temperatures. When greater than 1, tempered chains run concurrently within
each MCMC replicate, sharing the \code{num_threads} thread pool.}

\item{pt_grad}{Power to raise parallel tempering chains to. A value of 1
results in evenly distributed temperatures between [0,1], below 1 will bias
towards 1 and above 1 will bias towards 0. Only used if \code{pt_chains} is a
single value (i.e. not a vector).}

\item{num_threads}{Maximum number of TBB worker threads for one \code{run_mcmc()}
call. Defaults to physical cores minus one. With multiple parallel-tempering
chains, chain updates share this pool; inner parallelism is disabled to avoid
oversubscription. With a single chain, inner likelihood work may use the
pool instead.}

\item{pt_num_threads}{Deprecated alias for \code{num_threads}.}

\item{adapt_temp}{Logical indicating whether or not to adapt the parallel
tempering temperatures. If TRUE, the temperatures will be adapted during the
\code{burnin} period, starting after \code{pre_adapt_steps} steps. The adaptation will
occur every \code{temp_adapt_steps} steps until burnin is complete. The range of
temperatures will remain the same as specified by \code{pt_chains}.}

\item{pre_adapt_steps}{Number of steps to take before starting to adapt the
parallel tempering temperatures. Only used if \code{adapt_temp} is TRUE.}

\item{temp_adapt_steps}{Number of steps to take between temperature
adaptation steps. Only used if \code{adapt_temp} is TRUE.}

\item{max_initialization_tries}{Number of times to try to initialize the
chain before giving up}

\item{max_runtime}{Maximum runtime in minutes. If the MCMC is running for
more than this amount of time, the function will stop and return the current
state of the MCMC.}

\item{initial_allele_frequencies}{Optional list of initial allele frequencies
for each population. If provided, should be a list of length \code{num_populations},
where each element is a list of length \code{num_loci}, and each locus element is
a numeric vector of allele frequencies that sum to 1. If NULL, allele frequencies
will be initialized using clustering-based approach.}

\item{observation_model}{\code{"binary"} (default) for presence/absence error
process, or \code{"counts"} for Poisson count emissions.}
}
\description{
Sample from the target distribution using MCMC
}
\section{Parallelism}{

\code{run_mcmc()} uses Intel TBB (via RcppParallel) for C++ parallelism.
\itemize{
\item \strong{\code{num_threads}}: thread budget for one replicate. Leave unset for a
sensible default (\verb{physical cores - 1}).
\item \strong{\code{pt_chains}}: parallel-tempering replicas within one MCMC run. These are
statistical chains, not hardware threads; they share \code{num_threads}.
\item \strong{\code{num_chains} / \code{num_cores}}: optional independent MCMC replicates in
separate R processes. Keep \code{num_cores * num_threads} at or below your
physical core count.
}
}

